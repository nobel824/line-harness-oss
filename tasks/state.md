# state — 特典→オートウェビナー→無料相談 自動導線

最終更新: 2026-08-27 / フェーズ: **要件定義 完了・実装 未着手**

## 何をしているか

AI顧問アカウント（`tatsuki | AI顧問` @288pnjfn / 友だち1,002人 / accountId `db3ca401-29e5-4c36-9720-6fec783703ef`）で、
「特典を受け取った人にオートウェビナーを案内し、無料相談の予約まで自動化する」導線を作る。

- 要件定義書（正本）: `tasks/requirements-auto-webinar-funnel.md`
- レビュー用HTML: `tasks/_review/requirements-auto-webinar-funnel.html`
- **LINE 側の設定変更はまだ一切していない**（調査は読み取りのみ）

## 決まったこと（ユーザー決定・2026-08-27）

| 論点 | 決定 |
|---|---|
| Workers のプラン | **Paid**。cron の CPU 10ms 問題は解消 |
| 対象の特典キーワード | **`特典` の完全一致1本のみ**。他15本は今回載せない |
| 対象の特定方法 | 今後受け取る人だけ自動タグ（過去分のバックフィルはしない） |
| ウェビナーの器 | **既存の `AI × 𝕏攻略ウェビナー` を使い回す**。ただし `slug` は `test` から変更する |
| 追客本文の持ち方 | **DB に本文列を追加して管理画面から編集**（Phase 2） |
| リリース順序 | **Phase 1 で導線を先に流し、管理画面は Phase 2** |
| ウェビナー案内の出し方 | **特典 reply に同梱（0通）＋ 未クリック者に翌日 push** |

## 実データ（調査で確定）

- ウェビナー: `AI × 𝕏攻略ウェビナー` / slug `test` / **draft** / `durationSeconds` 3600 / 毎日19:00-23:00の5枠 / CTA は Spir外部URL / `showAtSeconds` 2700 / **videoPrefix は null**
- 動画素材: `~/Documents/Zoom/2026-08-27 16.54.00 達貴 奥田のZoomミーティング/video1125579345.mp4`
  **3,449秒（57分29秒）/ 1920x1080 / h264+aac / 89MB**。→ HLS 変換して R2 `line-harness-images` へ。`durationSeconds` は 3449 に直す
- LINE プラン: ライト（月5,000通）。今月 **2,989通** 消費済み（残 約2,000通）
- 既存タグ6個。`特典受け取り` 系のタグは**存在しない**（新規作成が要る）
- 特典配布は auto_replies 16本。**どれもタグを付けていない**

## レビューで出た critical（要件定義書に反映済み）

1. **追客6通の本文がコード直書き**（`webinar-followups.ts:67-109`）で、中身が別商品向け
   （「※21分・カメラ／マイクOFF」「AI導入診断」「15分枠」）。57分29秒のウェビナーと矛盾する
2. **`first_delay_minutes` / `second_delay_minutes` は第4の独立ステージ**
   （CTAクリック後フォーム未送信・`webinar-followups.ts:112-141`）。
   抽出に `webinar_ctas.form_id` が要るため、**CTA を外部URLのままにすると永久に発火しない**

## 実装時に踏みやすい罠

- `is_active` を 0→1 に戻すと `stage_enabled_at` が古いままで**過去の離脱者に毎分50件ずつバースト送信**される
- `submitted_no_booking_*` は `booking_menu_id` と `booking_url` の**両方**が必要（片方だけだと沈黙）
- 予約は2経路あり、**Meet が付くのは「ウェビナー個別相談」経路だけ**。一般予約経路はリマインド2通目が2時間前になる
- シナリオ本文に**生URLを書かない**（`auto:` リンクが量産され、タグもシナリオも紐付かない）
- 視聴画面の実体は `apps/worker/src/client/webinar/main.tsx`。`apps/liff/` 側は**未デプロイの旧実装**
- ローカルの `~/repos/ai-komon-line-harness` は2026-04の古いクローン。**本番の実体はこの OSS repo 側**

## 次にやること

要件定義書 §7 の Phase 1 の1番から。まず動画の HLS 変換とウェビナー設定の更新。

## 未決（着手は止めない）

- 訴求文言の確定（LINE ガイドラインの条文当てが要る。「3ヶ月でXフォロワー1万人」＋セミナー誘導は型に接近している）
- 段別転換率の実測後に追客の段数・遅延分数を調整
